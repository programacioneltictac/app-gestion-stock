{
  "name": "app-gestion-stock",
  "version": "1.0.0",
  "description": "Sistema de gestion de stock (backend Express + frontend React servido por Express en produccion)",
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm install --prefix backend && npm install --prefix react-frontend && npm run build --prefix react-frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix react-frontend"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "ISC"
}
